# Palantiny Chatbot Server
# FastAPI 및 ASGI 서버
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# 데이터베이스 (PostgreSQL 비동기)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Redis (4.x+ async 지원)
redis>=5.0.0

# 설정 및 환경변수
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# CSV 처리
pandas>=2.0.0

# LLM API (OpenAI)
openai>=1.40.0

# AWS DynamoDB (비동기)
aioboto3>=13.0.0

# 토큰 카운트 (128k context 관리)
tiktoken>=0.5.0

# LangGraph (파이프라인 v1/v2 공용)
langgraph>=0.2.0
langchain-core>=0.3.0

# HTTP 클라이언트 (DJMEDI API 호출)
httpx>=0.27.0

# BM25 엔티티 alias 매칭 (v2 파이프라인)
rank-bm25>=0.2.2

# PDF 텍스트 추출 (모노그래프 전처리)
pypdf>=4.0.0
